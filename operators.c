#include <stdio.h>
#include <stdbool.h>
/*
    When dividing an integer with another integer be wary of the remainder since it'll be truncated. others use floats
    float z = x / (float) y
*/

/*
    x = x * 4  is simmilar to  x*=4;
    x = x / 5  is simmilart to x/=5
    x  = %2  is simmilar to x%=2
*/

int main(void){
   
    return(0);

}