#include <stdio.h>
#include <math.h>



int main(void){
    double A = sqrt(9);  // squareroot
    double B = power(2, 4); // 2 power 4
    int c = round(3.14) // round off
    int D = ceil(3.14) // ceiling
    int E = floor(3.99) // floor
    double F = fabs(-100) // absolute value
    double G = log(3) // logarith
    double H = SIN() //sin
    double I = cos(45) // cosine
    double J = tan(45) // tan
    return(0);

}